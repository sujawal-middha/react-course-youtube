function Error() {
    return (
        <>
            <h1>new app</h1>
            <h1>new app</h1>
        </>
    )
}
//? Note - we can return only one element in one time but if we want to return two or even more then we need to use div or empty paranthesis<></> to return the two elements.
export default Error;